const sortCategory = (mainCategory, subCategory) => {
  if (mainCategory) {
    switch (mainCategory) {
      case '':

        break;
      default:
        console.log('no exist category');
        break;
    }
  }

  if (subCategory) {
    switch (subCategory) {
      case '':

        break;
      default:
        console.log('no exist category');
        break;
    }
  }
}

export default sortCategory;
